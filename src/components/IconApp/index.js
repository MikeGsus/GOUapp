import React from 'react'

import {
  Icon,
  IconContainer
} from './styled'

const IconApp = ({ logo }) => {
  return (
    <IconApp>
      <Icon src={logo} alt='MUR' />
    </IconApp>
  )
}

export default IconApp
